import React from "react"
import { Layout, Hero, About, Experience, Projects } from "../components"

export default () => (
  <Layout>
    <main>
      <Hero />
      <About />
      <Experience />
      <Projects />
    </main>
  </Layout>
)
