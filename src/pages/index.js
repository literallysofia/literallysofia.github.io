import React from "react"
import { Layout, Hero, About, Experience, Projects, Contact } from "../components"

export default () => (
  <Layout>
    <main>
      <Hero />
      <About />
      <Experience />
      <Projects />
      <Contact />
    </main>
  </Layout>
)
