PUBLIC_SITE_PASSWORD=

# Optional: Force enable password gate in development mode (defaults to false)
# PUBLIC_ENABLE_PASSWORD_GATE=true

# Password for encrypting/decrypting the CV PDF
# Used locally with: CV_PASSWORD=secret npm run encrypt-cv
# CV_PASSWORD=
